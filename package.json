{
  "name": "quadrados",
  "private": true,
  "workspaces": ["web"],
  "scripts": {
    "build": "npm run build:vercel -w web",
    "dev": "npm run dev -w web",
    "lint": "npm run lint -w web"
  }
}
